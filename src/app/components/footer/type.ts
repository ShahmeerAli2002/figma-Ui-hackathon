export interface MenuItemProps {
    text: string;
  }
  
  export interface MenuSectionProps {
    title: string;
    items: string[];
  }
  
  export interface SocialIconProps {
    src: string;
    alt: string;
  }