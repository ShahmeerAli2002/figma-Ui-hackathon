export interface HeroContentProps {
  title: string;
  description: string[];
  ctaText: string;
  ctaLink: string; // Updated to match the correct casing
}

export interface HeroImageProps {
  src: string;
  alt: string;
}
