export interface NavLinkProps {
    text: string;
  }
  
  export interface SocialIconProps {
    src: string;
    alt: string;
  }
  
  export interface NavigationLinksProps {
    links: string[];
  }